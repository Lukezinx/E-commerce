spring:
  application:
    name: E-Commerce
  datasource:
    url: jdbc:postgresql://localhost:5432/Ecommerce
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

api:
  security:
    token:
      secret: ${JWT_SECRET:minha-chave-secreta-de-desenvolvimento-123}